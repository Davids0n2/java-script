let valores = [ 3 ,1 , 4 , 5, 7 , 9 ]
valores.sort() // valores.sort() ordena os números em ordem crescente
for (let  resu = 0 ; resu < valores.length ; resu++) {
    console.log(`A posição ${resu} tem o valor de ${valores[resu]}`)
}